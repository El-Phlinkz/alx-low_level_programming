#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_square(int size)
{
int m = 0;
		int o = 0;

	if (n > 0)
	{
		for (; m < n; m++)
		{
			for (o = 0; o < m; o++)
				putchar(' ');
		putchar(92);
		putchar('\n');
		}
	}
	else
		putchar('\n');
