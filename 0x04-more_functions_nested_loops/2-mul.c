#include "main.h"

/**
 * mul - check next line
 * multiplles two integers
 * @a: an input integer
 * @b: an input integer
 * Return: product of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
